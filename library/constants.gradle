project.ext {
  // used by: library, sample, support-library, support-sample
  releaseVersionCode          = 16
  releaseVersion              = '2.5.2'
  javaVersion                 = JavaVersion.VERSION_1_8
  targetSdkVersion            = 30
  compileSdkVersion           = 30
  buildToolsVersion           = '30.0.3'

  // used by: library, sample
  minSdkVersion               = 9

  // used by: support-library, support-sample
  libSupportMinSdkVersion     = 16
  libSupportAnnotation        = '1.1.0'
  libSupportAppCompat         = '1.2.0'

  // used by: support-sample
  libSupportCoordinatorLayout = '1.1.0'
  libSupportRecyclerView      = '1.1.0'
  libSupportMaterial          = '1.3.0'
}
